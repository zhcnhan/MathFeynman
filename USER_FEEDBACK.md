# USER_FEEDBACK.md — 使用反馈记录（你 → 架构侧 → Euler 流水线）

> 验收期记录本。任何"用起来不对劲 / 很爽 / 想要"都值得记。
> 格式：日期 / 场景 / 现象 / 期望。填好后贴给架构侧 AI 分诊。

## 严重度
- 🔴 卡死 / 报错 / 判错
- 🟡 体验差 / 内容问题
- 🟢 建议 / 想要的新功能

## 记录

### [Day 1] 场景：仪表盘首次走查
- 🟡 **推荐起点学段错误**（架构侧观察）：全库 0 掌握时，5 个"可学"根节点中，系统推荐了
  **高中·一次函数的斜率**，而对从基础捡拾的用户应优先推荐**小学**内容。
  期望：推荐顺序按 `学段（小学→初中→高中→大学→AI）→ 图谱层序` 排序，同层可再按编号。
### [Day 1] 场景：学习会话点击节点
- 🔴 **点任意节点报 HTTP 500（database is locked）**：点击节点/开始学习大多失败。
  架构侧定位：LLM 讲解在数据库事务未提交时同步调用（60–100s），全程占 SQLite 写锁，
  并发写请求 5s 超时 → 500。
  **处置：架构侧热修 R7 已上线**（LLM 前先提交 + busy_timeout=30s）。验证方式：
  重启服务后单点一个节点、耐心等待（≤2 分钟），应能进入学习页。Euler 需复审（docs/09 R7）。
- 🔴 **页面直接显示 LaTeX 源码**（`$\frac{7}{9}-...$`、孤立 `$$` 乱码），公式无法阅读。
  架构侧定位：MdMath.tsx 渲染器零容错（行内公式不跨行、显示公式只认行首），LLM 输出的
  不规范 LaTeX 全按纯文本显示了。**处置：架构侧热修 R8 已重写渲染器**（容忍跨行/杂散 $$），
  vite 热更新后刷新页面验证；Euler 复审 + 补 prompt 输出纪律 + 清理已缓存脏讲解（docs/09 R8）。
### [Day 1] 场景：完整走查（掌握 primary.0101 四则运算 76 分 ✅）
- 🔴/🟡 **讲解末尾的自测问题（asks）仍显示原始 `$...$` 符号**：正文已正常（R8 生效），但
  "🤔 试着在心里回答…" 那几条没走数学渲染。原因：前端 asks 直接当纯文本渲染。
- 🟡 **太慢（三处）**：① 点击节点首次进入（讲解生成）② 重新生成讲解 ③ 答错后等提示、
  提交费曼口述。请求期间页面无任何反馈，像卡死。
  期望：能感知"程序在运行、已等 X 秒"；如果可以，整体加速。
- 🔴 **答对后偶发"找不到页面"**：刷新重答或故意答错一次再答对可恢复（复现率低）。
- 🟡 **复盘记录页没有返回按钮**：从达标页点"查看费曼复盘记录"进去后回不到仪表盘。
- 🟡 **费曼评分维度名是英文**（correctness/own_words…）：应显示中文标签。
- 🔴 **费曼首轮未达标→追问时报"会话不可用 500"**：昨天四则一次通过未暴露；今天分数节点
  首轮不过、进入 Socratic 追问时崩溃。架构侧定位：last_scores 存成"列表的列表"，
  与追问/二轮评分的 schema 类型不符（R10）。**处置：架构侧热修 R10 已上线**（追问传最近一轮卡、
  二轮评分传摘要 dict），回归 138 passed；重启后同一会话可直接重试口述。Euler 需补
  "首轮不过→追问→二轮" 回归测试（现为测试盲区）。
- 🔴 **练习连续答错 2 次报 500（会话不可用）**：故意答错触发"回炉讲解"时崩。架构侧定位：
  `_practice_reset_cycle` 是模块函数却被 `self.` 调用（M2 起存在），且该回炉分支从无测试覆盖
  （与 R10 同模式的盲区）。**处置：架构侧热修 R11 已上线**（两处调用去 self.，回归 139 passed）；
  重启后重试"答错 2 次 → 回炉"路径。Euler 需补回炉 E2E + 对 service/session.py 做分支覆盖审计
  （docs/09 R11）。
- 🔴 **点"继续下一关"报 500（TypeError: 'NoneType' not callable）**：配了 Key 时服务端
  "内容自续"入口把 AI 出稿器置成了 None（AI 出稿只接了 scripts 命令行，没接 /api 在线入口）。
  架构侧定位并热修 R13：新增 backend/app/ai/drafting.py（AI 出稿器收编到后端），selfextend
  有 Key 即用它；回归 168 passed。重启后重试"继续下一关"；Euler 需复审 + scripts 去重 +
  补在线接线测试（docs/09 R13）。
- （用户请在此继续添加走查感受……）

---

## 分诊约定（架构侧执行）
- 🔴 引擎/前端 bug → 派 Euler 修复（附复现步骤）
- 🟡 内容/体验 → 内容修正或 UI/逻辑打磨
- 🟢 建议/设计 → 架构裁决后进文档再排期
